stages:
  - build

windows_build:
  stage: build
  tags:
    - windows
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

  before_script:
    - cmake --version

  script:
    - cmake -S . -B build
    - cmake --build build --config Release
